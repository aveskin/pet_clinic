package ru.aveskin.petclinic.visit.controller;

/*
GET /api/visits – список визитов

GET /api/visits/{id} – визит по ID

POST /api/visits – создать визит (питомец + врач + дата)

PUT /api/visits/{id} – обновить визит (например, описание)

DELETE /api/visits/{id} – отменить визит
 */

public class VisitController {
}
